# 在飞牛 OS (fnOS) 自己做应用实现电视盒子（HDMI 直出电视）

## 背景

设备是零刻 mini N200 12G 内存的主机装了飞牛 NAS，电视已经和主机用 HDMI 线连接。

- 飞牛 APP 开发文档：https://developer.fnnas.com/docs/guide
- 参考：BaoTvBox → `C:\D\Code\FnKodi_TVBOX\bao-tv-box-master`（TVBox 协议与订阅语义）
- 参考：FN-kodi → `C:\D\Code\FnKodi_TVBOX\fn-apps-main\fn-kodi`（官方 Kodi FPK / HDMI）
- 基础镜像：`wjz304/kodi:latest`（Kodi 21 + DRM；入口为 `tini -- entrypoint.sh`）

## 任务目标

构建一个 fnapp：**单个 Docker 镜像**，只保留需要的东西。

- 保留 Kodi 播放与 HDMI/DRM 直出。
- 支持 TVBox 订阅接口：点播直链/解析、直播等。
- **不改 Kodi 源码**；能力以「Go 网关 + Kodi 视频插件」接入。
- Python 只做薄桥接；热路径用 Go。
- 极致性能：少进程、少依赖、可裁剪。

## 已确定交付策略（2026-08-08-r2）

| 项 | 结论 |
|----|------|
| V1 UI | **视频插件** `plugin.video.fntvbox`；默认 Estuary 即可完整使用 |
| 自定义 Skin | **已完成**并预装默认启用；步骤见 `docs/skin-fntvbox-实现步骤.md`；禁用后插件仍可在 Estuary 下使用 |
| 进程模型 | `tini` → entrypoint **保留 shell（禁止 exec 自己）** + gateway 看门狗 + Kodi 子进程；SIGTERM 先停 Kodi 再停 gateway |
| HTTP 超时 | 短请求与媒体代理 TTFB **拆分**两个配置项 |
| 网关端口 | 仅 `127.0.0.1:18765`；compose **禁止**映射到宿主机 |
| Android / DRPY | V1 不做 |

## 实现与部署文档（给 AI / 构建机）

按下列文档执行即可从零做到交付：

1. **[实现路线与部署.md](实现路线与部署.md)** — P0→P10 主路径、信号/看门狗、构建规范  
2. **[docs/api-contract.md](docs/api-contract.md)** — 网关 ↔ 插件 HTTP 契约（含 `apiVersion`、`lastError`、超时拆分、episodeId）  
3. **[docs/skin-fntvbox-实现步骤.md](docs/skin-fntvbox-实现步骤.md)** — Skin 专项（已完成）  
4. **[docs/deploy.md](docs/deploy.md)** — **设备部署与验收手册**（构建产物、装机、P9 清单）

**构建：** 请在 **Linux 构建机**执行 `./scripts/build-all.sh`（Windows 只提交源码）。

**完成定义：** Linux 构建机产出 FPK + 镜像 tar；fnOS 安装后 HDMI 出画；默认 `skin.fntvbox` 下插件完成 CMS 点播与直播；gateway 崩溃可自愈；18765 不对宿主机暴露；无 Android 组件。真机勾选见 `docs/deploy.md`。
